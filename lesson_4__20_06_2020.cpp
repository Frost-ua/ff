//#include<iostream>
//#include<math.h>
//#include "ctime"
//using namespace std;


//void main()

	/*unsigned int number = 0, sum = 0, mult = 1;
	srand(unsigned(time(NULL)));
	
	for (int i = 1; i <= 10; i++)
	{
		number = rand() % 17 + 1;
		cout << "i = " << i << "number = " << number << endl;
		sum  += i;
		mult *= i;
		
	}
		cout << "Summ = " << sum << endl;
		cout << "Mult = " << mult << endl;
*/



	/*int number = 0, sum = 0, j = 0;
	srand(unsigned(time(NULL)));
	
	for (int i = 1; i <= 10; i++)
	{
		number = rand() % 150 + 50;
		cout <<  "number = " << number << endl;
		if (number % 2 == 0)
		{
			j++;
		}
		
	}
		cout << "Amount of pair = " << j << endl;

	system("pause");*/

//{
//	double cap = 0, perc = 0, time1 = 0, per1 = 0, a = 0;
//	srand(time(NULL));
//	cout << "Start cap: " << endl;
//	cin >> cap;
//	cout << "Percent: " << endl;
//	cin >> perc;
//	cout << "Time: " << endl;
//	cin >> time1;
//	per1 = perc / 100;
//
//	for (int i = 0; i <= time1; i++)
//	{
//		 a = cap * per1;
//		cout << "monthly = " << a << endl;
//		cap += a;
//	}
//	
//	system("pause");
//
//}


	//	//int num = 0;	//int sum = 0;	//for (int i = 1; i = 1000; i++)	//{	//	num = rand() % 6 + 1;	//	sum += 1;	//	if (num == 2)	//	{	//		cout << "It was " << sum << " tries" << endl;	//		break;	//	}	//}




//Написати програму, яка виводит на екран 10 випадкових чисел у діапазоні від 0 до 17 і підраховує їх суму та добуток.
//Комп’ютер кидає кубик(за допомогою генератора випадкових чисел).Порахувати через скільки спроб випаде двійка.
//Написати програму, яка виводить на екран 10 випадкових чисел у діапазоні від - 50 до 200 і порахувати кількість парних
//Написати „банківську” програму, яка за отриманими даними (початковий капітал, процентна ставка і строк вкладу) виводить на екран суму, яка буде на рахунку за введений строк.




















